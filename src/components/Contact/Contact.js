import React from 'react';
import Header from '../Header/Header';

const Contact = () => {
    return (
        <section className="body-image">
            <Header></Header>
            <h1 style={{textAlign: 'center', marginTop: '4rem', color: 'tomato'}}>Contact Coming Soon...</h1>
        </section>
    );
};

export default Contact;